@page "/"
@using {{RootNamespace}}.SliceFactory.Services
@using {{RootNamespace}}.SliceFactory.Models
@using {{RootNamespace}}.SliceFactory.Components.Shared

<div class="sf-page">

    <!-- Success Banner -->
    @if (ShowSuccessMessage && !string.IsNullOrEmpty(SuccessMessage))
    {
        <div class="sf-success-banner animate-slide-in">
            <div class="d-flex align-items-center gap-3">
                <div class="success-icon-wrap">
                    <i class="bi bi-check-circle-fill"></i>
                </div>
                <div class="flex-grow-1">
                    <strong>@GeneratedFeatureName</strong> slice generated successfully
                </div>
                <button class="btn-close btn-close-white" @onclick="HideSuccessMessage"></button>
            </div>
        </div>
    }

    <div class="sf-layout">
            <!-- Left Panel: Configuration -->
            <div class="sf-config-panel">
                <div class="sf-panel-header">
                    <div class="d-flex align-items-center gap-2">
                        <div class="sf-panel-icon">
                            <i class="bi bi-sliders2"></i>
                        </div>
                        <div>
                            <div class="sf-panel-title">Slice Configuration</div>
                            <div class="sf-panel-subtitle">Configure the slice properties below</div>
                        </div>
                    </div>
                </div>

                <div class="sf-panel-body">
                    <EditForm Model="M" OnSubmit="HandleSubmit">

                        <!-- Section: Project -->
                        <div class="sf-section">
                            <div class="sf-section-label">
                                <i class="bi bi-folder-symlink me-1"></i>Project Configuration
                            </div>

                            <div class="sf-info-note">
                                <i class="bi bi-info-circle me-1"></i>
                                Solution root is detected automatically from your SliceFactory location.
                            </div>

                            <div class="row g-3">
                                <div class="col-md-6">
                                    <label class="sf-label">Directory <span class="sf-required">*</span></label>
                                    <DirectoryTreePicker
                                        KnownPaths="_directoryPaths"
                                        Value="@M.DirectoryName"
                                        OnPathSelected="@(async path => { M.DirectoryName = path; await TriggerDebouncedPreview(); })" />
                                    @if (!string.IsNullOrEmpty(M.DirectoryName))
                                    {
                                        <input type="hidden" @bind="M.DirectoryName" />
                                    }
                                </div>

                                <div class="col-md-6">
                                    <label class="sf-label">Module Namespace <span class="sf-required">*</span></label>
                                    <input class="form-control sf-input"
                                           value="@M.NameSpace"
                                           @oninput="@(async e => { M.NameSpace = e.Value?.ToString() ?? ""; await TriggerDebouncedPreview(); })"
                                           placeholder="Profile.Article" />
                                </div>

                                <div class="col-md-6">
                                    <label class="sf-label">Primary Key Type <span class="sf-required">*</span></label>
                                    <input class="form-control sf-input"
                                           value="@M.PkType"
                                           @oninput="@(async e => { M.PkType = e.Value?.ToString() ?? ""; await TriggerDebouncedPreview(); })"
                                           placeholder="string" />
                                    <div class="sf-hint">e.g., <code>string</code>, <code>int</code>, <code>Guid</code></div>
                                </div>
                            </div>
                        </div>

                        <!-- Section: Slice Types -->
                        <div class="sf-section">
                            <div class="sf-section-label">
                                <i class="bi bi-puzzle me-1"></i>Slice Types
                            </div>

                            <div class="mb-3">
                                <div class="form-check mb-1">
                                    <input class="form-check-input" type="checkbox" @bind="M.EnableListing" id="chkListing" />
                                    <label class="form-check-label" for="chkListing">Listing</label>
                                </div>
                                @if (M.EnableListing)
                                {
                                    <input class="form-control form-control-sm" placeholder='e.g. "Doctors"'
                                           value="@M.ListingName" @oninput="@(async e => { M.ListingName = e.Value?.ToString(); await TriggerDebouncedPreview(); })" />
                                }
                            </div>

                            <div class="mb-3">
                                <div class="form-check mb-1">
                                    <input class="form-check-input" type="checkbox" @bind="M.EnableForm" id="chkForm" />
                                    <label class="form-check-label" for="chkForm">Form</label>
                                </div>
                                @if (M.EnableForm)
                                {
                                    <input class="form-control form-control-sm" placeholder='e.g. "Doctor Profile"'
                                           value="@M.FormName" @oninput="@(async e => { M.FormName = e.Value?.ToString(); await TriggerDebouncedPreview(); })" />
                                }
                            </div>

                            <div class="mb-3">
                                <div class="form-check mb-1">
                                    <input class="form-check-input" type="checkbox" @bind="M.EnableAction" id="chkAction" />
                                    <label class="form-check-label" for="chkAction">Action</label>
                                </div>
                                @if (M.EnableAction)
                                {
                                    <input class="form-control form-control-sm" placeholder='e.g. "Disable Doctor"'
                                           value="@M.ActionName" @oninput="@(async e => { M.ActionName = e.Value?.ToString(); await TriggerDebouncedPreview(); })" />
                                }
                            </div>

                            <div class="mb-3">
                                <div class="form-check mb-1">
                                    <input class="form-check-input" type="checkbox" @bind="M.EnableSelectList" id="chkSelect" />
                                    <label class="form-check-label" for="chkSelect">Select List</label>
                                </div>
                                @if (M.EnableSelectList)
                                {
                                    <input class="form-control form-control-sm" placeholder='e.g. "Doctor Types"'
                                           value="@M.SelectListName" @oninput="@(async e => { M.SelectListName = e.Value?.ToString(); await TriggerDebouncedPreview(); })" />
                                }
                            </div>
                        </div>

                        <!-- Generate Button -->
                        <div class="sf-action-row">
                            @if (!IsFormValid())
                            {
                                <div class="sf-validation-hint">
                                    <i class="bi bi-exclamation-circle me-1"></i>
                                    Fill all required fields and select at least one slice type
                                </div>
                            }
                            <button type="submit" class="btn sf-generate-btn" disabled="@(!IsFormValid())">
                                <i class="bi bi-magic me-2"></i>Generate Slice
                            </button>
                        </div>

                    </EditForm>
                </div>
            </div>

            <!-- Right Panel: File Tree Preview -->
            <div class="sf-preview-panel">
                <div class="sf-panel-header sf-preview-header">
                    <div class="d-flex align-items-center gap-2 flex-grow-1">
                        <div class="sf-panel-icon sf-panel-icon-dark">
                            <i class="bi bi-folder-check"></i>
                        </div>
                        <div>
                            <div class="sf-panel-title">File Structure Preview</div>
                            <div class="sf-panel-subtitle">
                                @if (IsPreviewLoading)
                                {
                                    <span class="text-primary"><i class="bi bi-arrow-repeat spin-icon me-1"></i>Updating...</span>
                                }
                                else if (PreviewFiles != null && PreviewFiles.Any())
                                {
                                    <span>Live &middot; updates as you type</span>
                                }
                                else
                                {
                                    <span>Type above to see preview</span>
                                }
                            </div>
                        </div>
                    </div>
                    @if (PreviewFiles != null && PreviewFiles.Any())
                    {
                        <div class="preview-tabs-header">
                            <button class="sf-tab @(ActivePreviewTab == "files" ? "sf-tab-active" : "")" @onclick='@(() => ActivePreviewTab = "files")'>
                                <i class="bi bi-files me-1"></i>Files
                            </button>
                            <button class="sf-tab @(ActivePreviewTab == "placement" ? "sf-tab-active" : "")" @onclick='@(() => ActivePreviewTab = "placement")'>
                                <i class="bi bi-lightbulb me-1"></i>Placement
                                @if (PlacementGuidance?.HasConflicts == true)
                                {
                                    <span class="sf-tab-badge">!</span>
                                }
                            </button>
                        </div>
                    }
                </div>

                <div class="sf-preview-body">
                    @if (ActivePreviewTab == "files" || PreviewFiles == null || !PreviewFiles.Any())
                    {
                        <FilePreview PreviewFiles="@PreviewFiles" IsLoading="@IsPreviewLoading" />
                    }
                    else
                    {
                        <PlacementGuidanceView Guidance="@PlacementGuidance" IsLoading="@IsPreviewLoading" />
                    }
                </div>
            </div>
        </div>
</div>

@code {
    private string ActivePreviewTab = "files";

    private async Task HandleSubmit()
    {
        if (IsFormValid())
            await GenerateDynamicCode();
    }
}

<style>
    .sf-page {
        height: calc(100vh - 3.5rem);
        display: flex;
        flex-direction: column;
        background: #f0f2f5;
        overflow: hidden;
    }

    /* Success banner */
    .sf-success-banner {
        background: linear-gradient(90deg, #0a6640 0%, #0d8a55 100%);
        color: white;
        padding: 0.6rem 1.25rem;
        border-bottom: 1px solid rgba(255,255,255,0.1);
        flex-shrink: 0;
        font-size: 0.85rem;
    }

    .success-icon-wrap {
        font-size: 1rem;
        flex-shrink: 0;
    }

    /* Main layout */
    .sf-layout {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 0;
        flex: 1;
        overflow: hidden;
    }

    /* Config panel */
    .sf-config-panel {
        display: flex;
        flex-direction: column;
        background: #fff;
        border-right: 1px solid #e0e0e0;
        overflow: hidden;
    }

    .sf-panel-header {
        padding: 1rem 1.25rem;
        background: #fff;
        border-bottom: 1px solid #e8e8e8;
        flex-shrink: 0;
    }

    .sf-panel-icon {
        width: 2rem;
        height: 2rem;
        background: #eef2ff;
        color: #4f46e5;
        border-radius: 6px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 0.9rem;
        flex-shrink: 0;
    }

    .sf-panel-icon-dark {
        background: #252526;
        color: #9c9cff;
    }

    .sf-panel-title {
        font-weight: 600;
        font-size: 0.9rem;
        color: #1a1a2e;
        line-height: 1.2;
    }

    .sf-panel-subtitle {
        font-size: 0.72rem;
        color: #888;
        margin-top: 1px;
    }

    .sf-panel-body {
        flex: 1;
        overflow-y: auto;
        padding: 1.25rem;
        scrollbar-width: thin;
        scrollbar-color: #ddd #fff;
    }

    /* Sections */
    .sf-section {
        margin-bottom: 1.5rem;
        padding-bottom: 1.5rem;
        border-bottom: 1px solid #f0f0f0;
    }

    .sf-section:last-child {
        border-bottom: none;
    }

    .sf-section-label {
        font-size: 0.7rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        color: #888;
        margin-bottom: 0.85rem;
    }

    .sf-info-note {
        background: #f0f7ff;
        border: 1px solid #d0e8ff;
        border-left: 3px solid #3b82f6;
        border-radius: 4px;
        padding: 0.5rem 0.75rem;
        font-size: 0.78rem;
        color: #1d4ed8;
        margin-bottom: 1rem;
    }

    /* Form inputs */
    .sf-label {
        font-size: 0.8rem;
        font-weight: 600;
        color: #374151;
        margin-bottom: 0.35rem;
        display: block;
    }

    .sf-required {
        color: #ef4444;
    }

    .sf-input {
        font-size: 0.85rem;
        border-color: #e5e7eb;
        border-radius: 6px;
        padding: 0.45rem 0.75rem;
        transition: border-color 0.15s, box-shadow 0.15s;
    }

    .sf-input:focus {
        border-color: #6366f1;
        box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.12);
        outline: none;
    }

    .sf-hint {
        font-size: 0.72rem;
        color: #9ca3af;
        margin-top: 0.25rem;
    }

    .sf-hint code {
        background: #f3f4f6;
        padding: 0 3px;
        border-radius: 3px;
        font-size: 0.7rem;
        color: #6366f1;
    }

    /* Slice toggles */
    .sf-slice-toggles {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    .sf-toggle {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.65rem 0.85rem;
        border: 1.5px solid #e5e7eb;
        border-radius: 8px;
        cursor: pointer;
        transition: all 0.15s;
        background: #fafafa;
        user-select: none;
    }

    .sf-toggle:hover {
        border-color: #c7d2fe;
        background: #f5f3ff;
    }

    .sf-toggle-active {
        border-color: #6366f1;
        background: #eef2ff;
    }

    .sf-toggle-icon {
        font-size: 1rem;
        color: #9ca3af;
        width: 1.25rem;
        text-align: center;
        flex-shrink: 0;
    }

    .sf-toggle-active .sf-toggle-icon {
        color: #6366f1;
    }

    .sf-toggle-name {
        font-size: 0.82rem;
        font-weight: 600;
        color: #374151;
        line-height: 1.2;
    }

    .sf-toggle-desc {
        font-size: 0.7rem;
        color: #9ca3af;
        line-height: 1.2;
        margin-top: 1px;
    }

    .sf-toggle-check {
        color: #6366f1;
        font-size: 0.9rem;
        flex-shrink: 0;
    }

    .sf-selectlist-config {
        background: #f8fafc;
        border: 1px solid #e2e8f0;
        border-radius: 8px;
        padding: 0.85rem;
    }

    /* Action row */
    .sf-action-row {
        padding-top: 0.5rem;
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    .sf-validation-hint {
        font-size: 0.75rem;
        color: #f59e0b;
        background: #fffbeb;
        border: 1px solid #fde68a;
        border-radius: 4px;
        padding: 0.4rem 0.65rem;
    }

    .sf-generate-btn {
        background: linear-gradient(135deg, #6366f1 0%, #4f46e5 100%);
        color: white;
        border: none;
        padding: 0.6rem 1.5rem;
        border-radius: 8px;
        font-weight: 600;
        font-size: 0.9rem;
        transition: all 0.15s;
        box-shadow: 0 2px 8px rgba(99, 102, 241, 0.35);
        width: 100%;
    }

    .sf-generate-btn:hover:not(:disabled) {
        background: linear-gradient(135deg, #4f46e5 0%, #4338ca 100%);
        box-shadow: 0 4px 12px rgba(99, 102, 241, 0.45);
        transform: translateY(-1px);
        color: white;
    }

    .sf-generate-btn:disabled {
        opacity: 0.5;
        cursor: not-allowed;
        box-shadow: none;
    }

    /* Preview panel */
    .sf-preview-panel {
        display: flex;
        flex-direction: column;
        background: #1e1e1e;
        overflow: hidden;
    }

    .sf-preview-header {
        background: #252526;
        border-bottom: 1px solid #3c3c3c;
        padding: 0.75rem 1rem;
        display: flex;
        align-items: center;
        gap: 0.75rem;
        flex-shrink: 0;
    }

    .sf-preview-header .sf-panel-title {
        color: #ccc;
    }

    .sf-preview-header .sf-panel-subtitle {
        color: #666;
    }

    .sf-preview-body {
        flex: 1;
        overflow: hidden;
        display: flex;
        flex-direction: column;
    }

    /* Preview tabs */
    .preview-tabs-header {
        display: flex;
        gap: 2px;
        margin-left: auto;
        flex-shrink: 0;
    }

    .sf-tab {
        background: none;
        border: 1px solid transparent;
        border-radius: 4px;
        color: #888;
        font-size: 0.75rem;
        padding: 0.3rem 0.65rem;
        cursor: pointer;
        transition: all 0.1s;
        white-space: nowrap;
    }

    .sf-tab:hover {
        background: rgba(255,255,255,0.07);
        color: #ccc;
    }

    .sf-tab-active {
        background: rgba(99, 102, 241, 0.15);
        border-color: rgba(99, 102, 241, 0.4);
        color: #a5b4fc;
    }

    .sf-tab-badge {
        display: inline-block;
        background: #f59e0b;
        color: #1e1e1e;
        border-radius: 50%;
        width: 14px;
        height: 14px;
        font-size: 0.6rem;
        font-weight: 700;
        line-height: 14px;
        text-align: center;
        margin-left: 3px;
    }

    /* Animations */
    .animate-slide-in {
        animation: slideIn 0.25s ease-out;
    }

    @@keyframes slideIn {
        from { transform: translateY(-100%); opacity: 0; }
        to   { transform: translateY(0);    opacity: 1; }
    }

    .spin-icon {
        display: inline-block;
        animation: spin 1s linear infinite;
    }

    @@keyframes spin {
        from { transform: rotate(0deg); }
        to   { transform: rotate(360deg); }
    }
</style>
